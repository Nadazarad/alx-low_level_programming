#include "main.h"

/**
 * print_numbers - prints the numbers from 0 to 9
 *                 followed by a new line
 *
 * @i: the number
 *
 * return: Always 0
*/

void print_numbers(void)
{
	int i;

	if (i >= 48 && i <= 57)
		_putchar(i);
	_putchar('\n');
}
