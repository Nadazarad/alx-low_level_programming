#include "main.h"

/**
 * _strlen - gives the length of a string
 *
 * @s: a string
 *
 * Return: length of the string
*/

int _strlen(char *s)
{
	char *s = "My first strlen!";

	printf("%d", strlen(*s));

	return (0);
}
